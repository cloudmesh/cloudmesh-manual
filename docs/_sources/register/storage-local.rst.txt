register list sample --service=storage --kind=local

Configuration for Cloud=local Service=storage
---------------------------------------------

::

    cloudmesh:
      storage:
        {name}:
          cm:
            active: false
            heading: Storage A
            host: localhost
            label: local
            kind: local
            version: TBD
            service: storage
          default:
            directory: ~/a
          credentials:
            name: TBD
            key: ~/.ssh/id_rsa.pub

To register this service the following parameter ned to be defined::

    name


Timer: 0.1452s Load: 0.0185s (line_strip)
